# AGRISMART-AI---JaitRix

in backend folder, run the following command to install the required dependencies:

```bash
cd Backend
pip install -r requirements.txt
python .\model\train_crop_model.py

```

also create a .env file in the backend folder and add the following environment variables:

```
OPENWEATHER_API_KEY=your_openweather_api_key
```
```
